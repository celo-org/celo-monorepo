coverage:
  status:
    project:
      default:
        target: auto
        threshold: null
        removed_code_behavior: adjust_base
      celotool:
        paths:
          - packages/celotool
      protocol:
        paths:
          - packages/protocol
    patch: off
