coverage:
  status:
    project:
      default:
        target: auto
        threshold: null
        removed_code_behavior: adjust_base
      celotool:
        paths:
          - packages/celotool
      celocli:
        paths:
          - packages/cli
      sdk:
        paths:
          - packages/sdk
    patch: off
