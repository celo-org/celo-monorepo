import * as base from '@celo/base/lib/future'

// Exports moved to @celo/base, forwarding them
// here for backwards compatibility
export import Future = base.Future
export import toFuture = base.toFuture
export import pipeToFuture = base.pipeToFuture
