// Exports moved to @celo/base, forwarding them
// here for backwards compatibility
export {
  conditionWatcher,
  repeatTask,
  RepeatTaskContext,
  RetryTaskOptions,
  RunningTask,
  RunningTaskWithValue,
  TaskOptions,
  tryObtainValueWithRetries,
} from '@celo/base/lib/task'
