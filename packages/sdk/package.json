{
  "name": "@celo/sdk",
  "version": "0.0.1",
  "description": "Celo SDK configuration layer",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "author": "Celo",
  "license": "Apache-2.0",
  "keywords": [
    "celo",
    "blockchain",
    "sdk"
  ],
  "scripts": {
    "build": "tsc -b .",
    "build-verbose": "tsc -b . --traceResolution",
    "clean": "tsc -b . --clean",
    "test": "jest --runInBand",
    "lint": "tslint -c tslint.json --project ."
  },
  "dependencies": {
    "web3-core": "1.2.4"
  },
  "devDependencies": {
    "@celo/sdk-types": "1.0.0"
  },
  "engines": {
    "node": ">=8.13.0"
  }
}
