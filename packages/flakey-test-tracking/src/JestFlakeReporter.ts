// // Custom jest reporter that creates new github issues for flakey tests.
// // Requires jest-circus runner to rerun failed tests -- Tests that pass on rerun are flakey.

// const FlakeReporter = require('jest/);

// class FlakeReporter extends StreamlineReporter {
//   onTestResult(test, result) {
//     test.
//   }
//   // printTestFileFailureMessage(testPath, config, result) {
//   //   if (result.failureMessage) {
//   //     const trimmedMessage = HIERARCHY_REGEX_TRIMMER.exec(result.failureMessage);
//   //     this.log(trimmedMessage);
//   //   }

//   //   super.printTestFileFailureMessage(testPath, config, { ...result, failureMessage: null });
//   // }
// }

// module.exports = FlakeyTestDetoxReporter;
