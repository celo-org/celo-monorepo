{
  "name": "@celo/wallet",
  "version": "0.0.1",
  "description": "Celo Base Wallet layer",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "author": "Celo",
  "license": "Apache-2.0",
  "keywords": [
    "celo",
    "blockchain",
    "wallet"
  ],
  "scripts": {
    "build": "echo 'test for now'",
    "build-verbose": "tsc -b . --traceResolution",
    "clean": "tsc -b . --clean",
    "test": "jest --runInBand",
    "lint": "tslint -c tslint.json --project ."
  },
  "devDependencies": {
    "@celo/sdk-types": "1.0.0-dev"
  },
  "engines": {
    "node": ">=8.13.0"
  }
}
