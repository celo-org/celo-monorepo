{{ if and (not (eq (.Values.geth.validators | toString) "og")) .Values.geth.static_ips }}

{{ range $index, $e := until (.Values.geth.validators | int) }}

{{ $loadBalancerIP := index $.Values.geth (print "validators_" $index "IpAddress") }}

{{ template "celo.node-service" (dict "Values" $.Values "Release" $.Release "Chart" $.Chart "index" $index "service_type" "LoadBalancer" "node_type" "validators" "name_suffix" "" "component_label" "validators" "load_balancer_ip" $loadBalancerIP ) }}
  ports:
  - name: discovery
    port: 30303
    protocol: UDP
---
{{ template "celo.node-service" (dict "Values" $.Values "Release" $.Release "Chart" $.Chart "index" $index "service_type" "LoadBalancer" "node_type" "validators" "name_suffix" "-tcp" "component_label" "validators" "load_balancer_ip" $loadBalancerIP ) }}
  ports:
  - name: discovery
    port: 30303
    protocol: TCP
---
{{ end }}

{{ end }}
