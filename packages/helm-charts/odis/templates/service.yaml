apiVersion: v1
kind: Service
metadata:
  name: {{ include "odis.fullname" . }}
  labels:
    {{- include "odis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "odis.selectorLabels" . | nindent 4 }}
