apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-volume
spec:
  accessModes:
    - ReadWriteMany
  storageClassName:  {{ .Release.Name }}-nfs
  resources:
    requests:
      storage: 1Gi
